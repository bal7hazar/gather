{
  "name": "gather",
  "private": true,
  "version": "0.1.0",
  "description": "Gather — a solo, score-chasing spatial number game (full-client prototype).",
  "packageManager": "pnpm@11.0.5",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "preview": "turbo run preview",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
